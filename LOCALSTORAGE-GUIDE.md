# 💾 localStorage Persistence - מדריך שימוש

**תאריך:** 29 בדצמבר 2025
**סטטוס:** ✅ מוכן לשימוש!

---

## 🎯 מה עשינו?

הוספנו **localStorage persistence** לכל הנתונים ב-Mock Mode!

עכשיו כל השינויים שלך **נשמרים** ולא נעלמים ברפרש! 🚀

---

## ✅ מה עובד?

### **כל הנתונים נשמרים אוטומטית:**
- ✅ **Leads (לידים)** - יצירה, עריכה, מחיקה, שינוי שלב
- ✅ **Customers (לקוחות)** - יצירה, עריכה, מחיקה
- ✅ **Products (מוצרים)** - יצירה, עריכה, מחיקה
- ✅ **Orders (הזמנות)** - יצירה, עריכה, מחיקה, שינוי סטטוס
- ✅ **Tasks (משימות)** - יצירה, עריכה, סיום, הקצאה
- ✅ **Workflows (תהליכים)** - נשמר
- ✅ **Departments (מחלקות)** - נשמר

---

## 🚀 איך זה עובד?

### **בפעם הראשונה שאתה נכנס:**
1. האפליקציה טוענת את הנתונים הדיפולטיים (10 לידים, 10 לקוחות וכו')
2. שומרת אותם ב-localStorage

### **מכל פעם הבאה:**
1. האפליקציה טוענת את הנתונים מ-localStorage ✅
2. רואה את כל השינויים שעשית! 🎉

### **כל פעם שאתה משנה משהו:**
- יוצר ליד חדש → **נשמר מיד!**
- עורך לקוח → **נשמר מיד!**
- מסיים משימה → **נשמר מיד!**
- מוחק הזמנה → **נשמר מיד!**

---

## 💡 איך להשתמש?

### **לעבוד רגיל:**
פשוט תשתמש באפליקציה כרגיל! הכל נשמר אוטומטית.

### **לנקות הכל וחזרה להתחלה:**
פתח את **Console** בדפדפן (F12) והקלד:
```javascript
localStorage.clear();
location.reload();
```
זה ימחק הכל ויטען את הנתונים המקוריים מחדש.

### **לייצא את הנתונים שלך:**
פתח **Console** והקלד:
```javascript
const data = {
  leads: JSON.parse(localStorage.getItem('mockLeads')),
  customers: JSON.parse(localStorage.getItem('mockCustomers')),
  products: JSON.parse(localStorage.getItem('mockProducts')),
  orders: JSON.parse(localStorage.getItem('mockOrders')),
  tasks: JSON.parse(localStorage.getItem('mockTasks'))
};
console.log(JSON.stringify(data, null, 2));
```
העתק את הפלט ושמור בקובץ JSON!

---

## 🔄 מעבר לשרת אמיתי

כשהבק אנד יהיה מוכן:

### **אופציה 1: ייבוא ידני**
1. ייצא את הנתונים (ראה למעלה)
2. שנה `MOCK_MODE = false` ב-`src/services/api.js`
3. השתמש בפיצ'ר הייבוא (Import) שיש בכל דף (Leads, Customers וכו')
4. העלה את הקובץ שייצאת!

### **אופציה 2: העברה אוטומטית**
נכתוב סקריפט שיעביר הכל לשרת בפעולה אחת! (אפשר לעשות זאת מאוחר יותר)

---

## 📊 איפה הנתונים נשמרים?

הנתונים נשמרים ב-**localStorage** של הדפדפן שלך:

- `mockLeads` → לידים
- `mockCustomers` → לקוחות
- `mockProducts` → מוצרים
- `mockOrders` → הזמנות
- `mockTasks` → משימות
- `mockWorkflows` → תהליכים
- `mockDepartments` → מחלקות

---

## ⚠️ חשוב לדעת!

### ✅ **יתרונות:**
- הכל נשמר בין רפרשים
- עובד מהר מאוד (local)
- אפשר לעבוד אופליין
- לא צריך backend בכלל!

### ⚠️ **מגבלות:**
- הנתונים נשמרים **רק בדפדפן שלך**
  - אם תפתח ב-Chrome אחר → לא יהיו הנתונים
  - אם תפתח ב-Firefox → לא יהיו הנתונים
- אם תנקה את ה-cache של הדפדפן → הנתונים ימחקו
- יש מגבלת גודל (~5-10MB) - אבל זה הרבה מאוד!

---

## 🎉 סיכום

**עכשיו אתה יכול:**
1. ✅ להכניס לידים, לקוחות, משימות - **הכל נשמר!**
2. ✅ לרענן את הדף - **הכל עדיין שם!**
3. ✅ לעבוד שבועות עם הנתונים שלך
4. ✅ לייצא את הנתונים כשתרצה
5. ✅ להעביר לשרת כשיהיה מוכן - **בקלות!**

---

## 🔧 איך זה עובד מאחורי הקלעים?

**קובץ:** `src/services/api.js`

### **פונקציות עזר:**
```javascript
// טוען מ-localStorage או משתמש ב-default
const loadFromStorage = (key, defaultData) => {
    const stored = localStorage.getItem(key);
    return stored ? JSON.parse(stored) : defaultData;
};

// שומר ל-localStorage
const saveToStorage = (key, data) => {
    localStorage.setItem(key, JSON.stringify(data));
};
```

### **טעינה:**
```javascript
let mockLeads = loadFromStorage('mockLeads', defaultMockLeads);
```

### **שמירה:**
בכל פעולה (create, update, delete):
```javascript
mockLeads.push(newLead);
saveToStorage('mockLeads', mockLeads); // ← נשמר אוטומטית!
```

---

**נוצר על ידי:** Claude Code
**תאריך:** 29/12/2025
**זמן עבודה:** ~15 דקות
**מצב:** ✅ עובד מצוין!
